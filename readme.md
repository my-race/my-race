# my Race

## 러닝 대회 정보와 알림

```
my-monorepo/
├── apps/
│   ├── web/         # Next.js (React DOM)
│   └── mobile/      # React Native (Expo 또는 CLI)
├── packages/
│   ├── ui/          # 공통 UI 컴포넌트
│   └── utils/       # 공통 유틸
├── package.json     # 루트 설정 (yarn workspaces + scripts)
├── turbo.json       # turborepo 설정
└── tsconfig.json    # tsconfig paths 공유 (선택)
```